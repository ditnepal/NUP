import { PrismaClient } from "@prisma/client"; const prisma = new PrismaClient({ datasources: { db: { url: "file:/prisma/dev.db" } } }); async function main() { console.log("Users:", await prisma.user.count()); console.log("Members:", await prisma.member.count()); console.log("Configs:", await prisma.systemConfig.count()); } main().finally(() => prisma.$disconnect());
